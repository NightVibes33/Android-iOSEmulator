# macOS 26 verification

This branch exists only to expose the pull-request workflow run through the connected GitHub tooling. The file can remain as a record of the first real-device unsigned IPA build verification.
