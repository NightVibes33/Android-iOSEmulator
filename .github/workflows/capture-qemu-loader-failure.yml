name: Capture LiveContainer serial-signer failure

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/capture-qemu-loader-failure.yml"
  workflow_dispatch:

permissions:
  actions: read
  contents: write

jobs:
  capture:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Capture failed packaging step only
        env:
          GH_TOKEN: ${{ github.token }}
          FAILED_RUN_ID: "29981195849"
          FAILED_JOB_ID: "89123126576"
        run: |
          set -euo pipefail
          mkdir -p .github/release-status
          curl --fail --location --silent --show-error \
            -H "Authorization: Bearer $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/repos/$GITHUB_REPOSITORY/actions/jobs/$FAILED_JOB_ID/logs" \
            -o /tmp/job-logs
          if file /tmp/job-logs | grep -qi zip; then
            unzip -p /tmp/job-logs > /tmp/job.txt
          else
            cp /tmp/job-logs /tmp/job.txt
          fi
          {
            echo "failed_run_id=$FAILED_RUN_ID"
            echo "failed_job_id=$FAILED_JOB_ID"
            awk '/APP=package\/Payload\/LiveContainer.app/{wanted=1} wanted{print}' /tmp/job.txt | tail -n 180
          } > .github/release-status/livecontainer-serial-signer-failure.log
          git fetch origin main
          git checkout -B main origin/main
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add .github/release-status/livecontainer-serial-signer-failure.log
          git commit -m "Capture LiveContainer serial-signer package failure" || exit 0
          git pull --rebase origin main
          git push origin HEAD:main
